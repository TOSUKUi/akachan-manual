# 05. 掲載URLと価格の裏取り記録（2026-09-02 実施）

`items/*.md` が指すURLの生死と、価格レンジの実在を機械的に検証した記録。spec 0003 の AC-4 / AC-5 / AC-7 に対応する。

## 検証方法

- **URL生死**: 掲載中の全URL（106件）を `curl -A "Mozilla/5.0"` で取得し HTTPステータスを確認。Python `urllib` は Amazon で 503、一部URLで不正リダイレクトを出すため、測定は必ず curl に統一した。
- **価格裏取り**: 価格をもつ45品目について、引用元HTML内に `low` / `high` の金額（`¥1,000` / `￥1,000` / 生数値）が実在するかを正規表現で確認。Amazon は検索結果をサーバー側でHTMLに焼き付けるため金額が本文に出る。楽天は SSR に価格を含さず、埋め込みJSON中の `DSP_PRICE` 実売額（税込み）を読む必要があった。アカチャンホンポ（`pageData` JSON 内 `price`）・西松屋（SSRに `¥`）は本文から抽出。
- 取得HTMLはキャッシュし、再実行時に再 fetch しない。

## 結果

- **価格: 45/45 品目が引用元HTML内の金額と一致**（下限・上限とも実在する値のみ採用）。
- **リンク: 31件が死亡**。内訳と対処は下記。

## 死亡URLの内訳と対処

| 種別 | 件数 | 対処 |
|---|---|---|
| `www.nishimatsuya-website.net`（存在しないドメイン） | 18 | 実ECドメイン `www.24028-net.jp/item_list.html?searchbox=1&q=` の検索URLへ |
| Amazon 商品直リンク `/dp/<ASIN>` | 32 | 検索URL `https://www.amazon.co.jp/s?k=…` へ。価格出典も同じ検索ページに張り替え、金額を再取得 |
| ピジョン `…/qa/` | 2 | 生存ページへ差し替え（`baby-feeding/`、`column/magp/091nasalcare.html`） |
| ピジョン `baby-feeding/food_9.html` | 1 | `baby-feeding/`（離乳食・授乳サポート）へ |
| ピジョン `baby/weaning-steps.html` / `column/food/009foods.html` | 2 | `baby/getsurei/getsurei-{3,5,8,12}.html`（離乳食開始月齢の実ページ）へ |
| 西松屋プレママ `/guide/{stroller,bath,diaper,babyroom,clothes,milk}/` | 6 | 生存する `/premama/preparation-item/*`・`/premama/guide/bedding/`・検索URLへ |
| ユニクロ `/jp/ja/products/search?q=` | 1 | `/jp/ja/search/?q=`（実測で検索が機能する形式） |
| 品川区 `hpg…`（別コンテンツへのリダイレクト含む） | 複数 | 下記 canonical path へ |

## 品川区URLについて（重要）

- `/KK/kodomo/…` 形式は `/PC/kodomo/…` 同名ページへリダイレクトされる（内容は同一）。**`/PC/` を正とする。**
- `hpg000000798`（乳幼児健康診査・相談）が持つリンクは `hpg000033502`（4か月児）、`hpg000033504`（9〜10か月）、`hpg000033505`（1歳6か月）、`hpg000033506`（2歳6〜3歳未満）、`hpg000033507`（病後児）、`hpg000033509`（病児）で、これが site 内の正式な系列。
- 検証実行日は当 egress IP に対して品川区サイトがHTML形式の404ページを返す状態（`robots.txt` も同様）で、`/PC/kodomo/kodomo/…` 形式の旧URLがその状態では通らなかった。**ページ本文を直接確認できた時点で取得した事実**（4か月児健診の引き換え品＝絵本・絵本バッグ、1歳6か月児健診＝食器・エプロン、2歳6か月健診＝ことばの絵本・おむつライブラリー、見守りおむつ定期便＝令和8年4月開始申請受付中、妊婦のための支援給付＝妊娠時5万円／出産後5万円、養育家庭のホームアシスト事業の助成＝里帰りに10万円上限）は保持し、URLは上記 canonical に統一した。

## 価格を持たせない品目

`nb-diaper-cream`（排泄の間隔を数える／おしりふきの補助）は、アカチャンホンポ・西松屋・Amazon の検索で「おしりふき」以外の該当品が出ず、根拠にできる金額がなかった。**金額を作らず price 無し**（件数・準備完了の状態にだけカウント）。

## 再発防止（ビルドゲート）

- `items/*.md` 1ファイル内で完結する出典参照チェックを `src/lib/items-parse.ts` の `checkSourceReferences` として実装し、解析時と `validateItems` の両方で流す。
  - 参照されているのに未登録 → `items_source_not_registered`
  - 登録されているのに誰からも参照されない → `items_source_unreferenced`（生きたURLを登録だけして放置する事故を防ぐ）
- 価格の調査日が180日を超えたらビルド警告（既存の fact 鮮度警告と同じ閾値）。

---

# 第二次検証（2026-09-02 / 品目の網羅と価格の再導出）

前回の死URL除去とは別に、「月齢ごとの必要品が薄い（出生〜2歳が 4〜5 品目だけ）」という指摘を受けて、
品目を増やしたうえで**価格をすべて実ページから再導出**した。

## 追加した視点（01-monthly-needs.md の再調査）

- 西松屋「プレママ・プレパパの準備リスト」（`/premama/preparation-item/*`, `/premama/guide/*`）
  … 数量目安（ベビーソープ1個・ボディポンプ1個・保湿1本・おしりふき5〜6個・帽子1枚・肌着の点数 等）が本文に書かれており、`size` の根拠にした。
- アカチャンホンポ 月齢別ガイド（`/shop/pages/hadacare/`・`/shop/pages/firstfood/`）と商品カテゴリ一覧（344件を機械抽出）
- ピジョン はじめての育児 月齢別（`baby/getsurei/getsurei-{0,1,3,4,5,6,7,8,11,12,16,20}.html`）と育児用品一覧（`baby/products/{wash,and,ba,muryo,play}/`）
- 西松屋 / アカチャンホンポ の**商品カテゴリページ**（価格がHTMLに直に埋め込まれており機械抽出できる）

## 追加した品目（16品目。46 → 62）

| band | 追加item id |
|---|---|
| newborn | nb-diaper-bin / nb-nail-clipper / nb-mitten / nb-socks / nb-bath-thermo |
| m2-3 | m23-diaper-caddy / m23-detergent |
| m4-6 | m46-weaning-kit / m46-sun-hat |
| m7-9 | m79-chair-cover / m79-picturebook |
| m10-12 | m1012-cup / m1012-first-shoes |
| m13-18 | m1318-second-shoes |
| m19-24 | m1924-potty / m1924-storage |

既存品目の内、中身と名前がずれていた2件をリネームした（`m46-spoon` → `m46-tableware`＝食器セット、`m79-cotton-buds` → `m79-hygiene-kit`＝綿棒＋爪切り＋体温計セット）。

## 価格の導出方法（改訂）

1. 各itemの `price.sources` をHTTP取得し、**金額表記だけを抽出**（`￥1,234` / `1,234円` / Amazon は `a-price-whole`、楽天は `"DSP_PRICE":"1234"`）。ページ chrome（送料込み目安 3,980円・5,500円など）は除外。
2. 観測値の10〜90パーセンタイルを `low` / `high` に採用（外れ値に引っ張られないため）。
3. **`low` と `high` が両方ともそのページに実在すること**をゲートにする（以前の「文字列に含まれていればOK」は、別の数字に偶然一致する偽陽性を通していたため廃止）。
4. `low == high` になったものは、同カテゴリの別価格を拾ってレンジに直した
   （m23-nail-guard 110→110〜437 / m1012-hat 1320→1100〜1518 / m1318-shades 1848→1650〜2200 / m1924-storage 4180→3300〜4980）。
5. レンジが2倍以上開いた品目は、カード内に「幅が大きい」の注記を出す（AC-2）。

### 再導出後の主だったレンジ（調査日 2026-09-02）

| item | range | 出典カテゴリ |
|---|---|---|
| m46-chair | 4,741〜23,575円 | 西松屋 `MEAL_CHAIR` |
| m79-bath-mat | 2,580〜8,660円 | 西松屋 `BATH_BATHMAT` |
| m1012-first-shoes | 2,970〜5,980円 | アカチャン `cb086` |
| m1318-second-shoes | 1,980〜4,780円 | アカチャン `cb087` |
| m1924-roleplay | 4,055〜13,599円 | アカチャン `cb181` |
| nb-diaper-bin | 272〜3,899円 | アカチャン `cb317`（消臭袋を含む） |

## 販売先リンクの形式（実測で確定）

| 販売先 | 形式 | 備考 |
|---|---|---|
| 西松屋 | `https://www.24028-net.jp/item_list.html?searchbox=1&q=<語>` | `/search/?q=` と `/products/search?keywords=` は**404**。`item_list.html` 形式のみ検索結果が描画される（本文はJS描画なので価格は埋め込みHTMLには出ない。価格はカテゴリページ側から抽出） |
| アカチャンホンポ | `https://shop.akachan.jp/shop/goods/search.aspx?keyword=<語>` | `/shop/search?keyword=` は404。`goods/search.aspx` が正規の検索エンドポイント。カテゴリページ `/shop/c/cbNNN/` は常に200で価格もHTMLに含まれる |
| Amazon | `https://www.amazon.co.jp/s?k=<語>` | ボット検知で 200 + 数KBの警告ページを返すことがある。50KB未満の応答は「取得失敗」として再試行 |
| 楽天 | `https://search.rakuten.co.jp/search/mall/<語>/` | `DSP_PRICE` が実売 |

## 参照を見送った販売先

- **ベビーザらス（`babiesrus.co.jp`）**: トップページは200だが、検索URLが `/ja-jp/cheq/secure-page/`（ボットチェック）へリダイレクトされ、リンク先として検証できない。
- **西松屋ベビーザらス株式会社 / BIRTHDAY（`birthday.co.jp`）**: 403で機械検証不可（`birthday.bz` はそもそも名前解決しない）。
- 検証できないリンクを「出典・販売先」として掲げるのは、誤リンク・死リンクを量産した前回の失敗そのものなので、**今回はどちらも採用しなかった**。人手で開けることが確認できるなら `SHOP_KINDS` に追加すればよい。

## 品川区サイトの404について（前回レポートの訂正）

前回「当 egress IP に対して404を返す状態」と書いたのは、**同一URLが数秒おきに200と404を交互に出す不安定な状態**だった（20回中6回404、HTML長2,254Bの404ページ／`Server: CloudFront`）。削除ではない。再試行で200が確認できたため、`hpg000032664`（児童手当）、`hpg000020943`（子ども医療費）、`hpg000033807`（産後ケア）、`hpg000000655`（ファミリー・サポート・センター）、`hpg000020024`（ファーストサイン教室）、`hpg000000798`、MHLWリーフレットPDF はすべて生存として登録を継続。
**教訓: 1回の404でリンクを削除しない。再試行してから判断する。**

---

# 第三次検証（2026-09-02 / `/timeline` の端末別レイアウト実測）

## 方法

ビルド済み `dist/` を `vite preview`（`0.0.0.0:4173`）で配信し、Chromium（Playwright）で
**クリック・スクロール・リロードまで実操作**して検証した。DOMをソースから読み解く近似ではなく、
実測値で判定する（`getBoundingClientRect` / `scrollY` / `localStorage` を直接読む）。
検証ビューポートは 390×844（モバイル）・768×1024（タブレット）・1440×900（デスクトップ）。72項目 PASS、コンソールエラー 0件。

## 実測でわかったモバイルの問題と対処

| 実測 | 問題 | 対処 |
| --- | --- | --- |
| 375px でカテゴリ行 232px + 月齢レール 488px = **400px（画面の47%）** | 品目を見るまでにスクロールが要る | 月齢ラベルを compact 化（月齢範囲のみ）して4列グリッド → **172px**。カテゴリ行は横スクロール1行 → **76px** |
| `--header-h: 161px`（`body[data-page]` 適用） | `Layout` が `data-page` を描画しておらず **セレクタが一度も当たっていなかった**。実測はモバイル 53px / デスクトップ 191px | `:root` + `@media (min-width:1024px)` に一本化し、実測値 53px / 191px に修正 |
| 月齢 chip 選択後の自動スクロールが **band 先頭の360px手前**で止まる | 他の band が閉じる前（文書高さ 59,664px）の位置で目標を計算していた | `src/lib/scroll.ts` で **文書高さが収束してから**目標を計算（閉じた後は 7,938px）。着地誤差 12px |
| 固定帯の下端計算が sticky 要素の未吸着位置（562px）を読む | 着地位置が大きくずれる | sticky + `top` 指定は「吸着後の位置（`top` + 高さ）」で計算、非表示（高さ0）の帯は除外 |
| チェックボックスのタップ領域が 24px | WCAG の目標サイズ（24×24px 以上／入力は44px推奨）に不足 | label でラップして **44×44px**（視覚サイズは24pxのまま）。タップ可能領域を自動計測して全品目で44px以上を確認 |

## 追加したUI

- **モバイル専用の吸着サマリーバー**（`data-testid="mobile-summary"`）: 残り品数と残価額を1行で常時表示。
  幅が狭いため `compactYenRange()` で `10.3万〜48.9万円` 表記に短縮（**数値はデスクトップのサマリーカードと同一算出**）。
  ヘッダー込みで 101px = **画面の12%**（20%以下）。`aria-live="polite"` で変化を読む。
- **「月齢えらびにもどる」**: 月齢レールが画面外に出た状態からレールへ戻す（スクロール量960pxを実測）。
- デスクトップは品目を**2列グリッド**化して品目カードの高さを約19%削減（410px → 351px）。

## 不変の確認（リグレッション）

- 初回は全 band が開いている。月齢を選ぶと選択 band だけ開き、`すべて見る` で全開に戻る。
- 価格レンジはデータ算出（`¥102,925〜¥489,911` = 価格つき70品目の low/high 合計）と一致。
- 完了一覧は band ではなく品目名（チェックで「帽子」のみ、月齢 chip は消えない）。
- チェック状態はリロード後も復元（`items-timeline:v1` v2 form）。リロードで月齢選択は引き継がず全開。
- 「2歳以上」band は廃止済み（月齢 chip は8個）。継続利用は band 見出し下と品目ごとの
  「2 歳以降も継続」で示す。ピジョンの A形ベビーカー例示（36か月）は本文で **3歳** と表記し、
  月齢 chip と矛盾させない。

## 追加リンク実測（2026-09-03 07:00 JST・全135URLの再帰走査）

デプロイ済みページに出ているリンクを全数再走査し、**リダイレクト先が別ドメインになる URL と
死亡ホスト**を潰した（品目ごとの出典は張替え後もすべて LIVE を再確認）。

| 種別 | 対応 |
| --- | --- |
| `www.pigeon.info`（`Location: https://pigeon.info/...` で毎回301） | 全30箇所を `pigeon.info` へ正規化（ピヨコミラーの実URL） |
| `pigeon.info/products/item/920`（ベビーワセリン） | `products.pigeon.co.jp` へ外部転送されるため、解説として `pigeon.info/baby/care/newborn-care/` に張替え |
| 液体ミルク（非常用備蓄）の why 出典 | 当初は沐浴ページを参照していた。差し替えた hpg000045106 は 2026-09-03 実測で **HTTP 404**（ページ移転・削除）のため、東京都福祉局「災害時に備えて 知っていますか？乳児用液体ミルク」＋品川区「区民避難所の機能充実・運営組織」（備蓄倉庫に液体ミルク・ベビーフードと記載）へ再張替え |
| `http://www.city.shinagawa.tokyo.jp/...` | `https://` へ修正（http は https へ301） |
| 廃止された内部検索ホスト（旧 `nishimatsuya-network.com` / 旧 `search.akachan.jp` 系） | 引用から完全削除。現在の店舗導線は `src/lib/shop-links.ts` の検索URL生成のみ（実HTTPで 200 / 683件・966件のヒットを確認） |

走査後の状態: **LIVE 134 / 意図的除外 1**（リダイレクトで別ドメインになる URL は引用から除去済み）。

## 7. サイズアウト／切り替えアイテム追加時のリンク・価格検証（2026-09-03 07:00 JST）

月齢の目盛り不足（服のサイズ買い足し・おむつタイプ切替など）を埋めるため 10 品目を追加し、
その根拠 URL を新規登録した（品目数 71 → 81、価格つき 70 → 78）。

| 検証項目 | 方法 | 結果 |
| --- | --- | --- |
| 西松屋サイズ表（50〜90cm と参考月齢） | `24028.jp/premama/guide/babywear/` を取得し、サイズ表のテーブルを実確認 | 50/60/70/80/90センチ ＝ 新生児/3か月前後/6か月前後/12か月前後/24か月前後 |
| サイズ別実売価格（60・70・80・90） | `24028-net.jp/category/{S85,S3,S4,S5}/` を**実ブラウザで描画**してから税込価格を全件抽出（静的HTMLには商品が出ないため curl 不可） | 60cm＝328〜1,307円 / 70cm＝328〜1,307円 / 80cm＝328〜977円 / 90cm＝328〜977円（各100点前後の価格表示） |
| 哺乳びん乳首の月齢目安 | `pigeon.info/baby-feeding/` の乳首サイズ表 | S=0か月〜 / M=4か月頃〜 / Y（スリーカット）=6か月頃〜 / L=9か月以上〜 / 3L=15か月頃〜 |
| 乳首の実売価格 | `24028-net.jp` 検索 `q=乳首` をブラウザ描画して税込抽出 | 658〜1,846円（商品検索の価格表示。送料込表示は除外） |
| おむつのテープ／パンツ切替根拠 | `24028.jp/premama/guide/diapers/` | 「ねんねが中心の時期に便利」／「ハイハイやたっちができて動き回るようになると、パンツタイプが便利」 |
| A型・B型ベビーカーの時期 | `24028.jp/premama/preparation-item/childcare/` | 「生後1ヶ月から使用できるA型」／「お座りができるようになる7ヶ月頃から使えるB型（バギータイプを含む）」 |
| チャイルドシートの前向き切替 | `24028.jp/premama/guide/childseat/` | 「15ヶ月かつ身長76cmを超えるまでは後ろ向き」、6歳未満使用義務、140cm未満はジュニアシート推奨 |
| 乳幼児の事故・安全対策 | `pigeon.info/baby/theme/room.html` ／ `nite.go.jp` | 「家中すべてがチェック対象」「フェンスなどでガード」「誤飲事故に注意」 |

- 全 141 URL を再走査（`.urlcheck/results_now.json`、`.urlcheck/urls.txt` を現行データから再生成）: **dead 0 / 別ドメインリダイレクト 0**。
- 新登録の `fukushi.metro.tokyo.lg.jp` は公的一次ソースのため、リンク許可リスト（`src/pages/__tests__/timeline-page.test.tsx`）に `metro.tokyo.lg.jp` を追加。
- 注意: 西松屋の税込価格はセール可否で変動する。サイズ別レンジはカテゴリページ表示時点の実売で、**「この価格で買える」という保証ではなく目安**である旨を価格脚注で示している。
